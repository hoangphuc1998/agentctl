# AGENTS.md

Linux desktop app for managing `agentctl` coding-agent runs.

## Python runtime

Always run Python code with `uv run` whenever Python execution is needed.

## Verification

Use `./init.sh` as the standard local verification path.

